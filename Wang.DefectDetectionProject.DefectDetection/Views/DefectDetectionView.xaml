<UserControl x:Class="Wang.DefectDetectionProject.DefectDetection.Views.DefectDetectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:controls="clr-namespace:Wang.DefectDetectionProject.Shared.Controls;assembly=Wang.DefectDetectionProject.Shared"
             xmlns:local="clr-namespace:Wang.DefectDetectionProject.DefectDetection.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- ListBox 整体样式 -->
        <Style x:Key="ImageListBoxStyle" TargetType="{x:Type ListBox}">
            <!-- 基本外观 -->
            <Setter Property="Background" Value="#8E95A3"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>

            <!-- 容器模板：带圆角边框 + 滚动查看器 -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                            <ScrollViewer x:Name="ScrollViewer"
                                      Padding="{TemplateBinding Padding}"
                                      Focusable="False">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <!-- 子项容器样式 -->
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Padding" Value="6"/>
                        <Setter Property="Margin" Value="3"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>

                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="6"
                                        Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <!-- 悬停状态 -->
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#E7F0FF"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="#A3C4F3"/>
                                        </Trigger>

                                        <!-- 选中状态 -->
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#3A86FF"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="#2F6FD9"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="TextElement.Foreground" Value="White"/>
                                        </Trigger>

                                        <!-- 选中且悬停 -->
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Bd" Property="Background" Value="#2F6FD9"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="#1E4F9C"/>
                                        </MultiTrigger>

                                        <!-- 禁用状态 -->
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Foreground" Value="#ADB5BD"/>
                                            <Setter Property="TextElement.Foreground" Value="#ADB5BD"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="0.3*" />
        </Grid.ColumnDefinitions>

        <Grid Margin="0, 10, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="0.3*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <controls:ImageEditView DrawingObjectList="{Binding DrawingObjectList, Mode=TwoWay}" Image="{Binding CurrentImage}" HWindow="{Binding HWindow, Mode=TwoWay}"/>

            <ListBox Grid.Row="1" x:Name="imgListBox" SelectionMode="Extended" 
                 ItemsSource="{Binding DefectDetectionService.ImageListItems}" 
                 Style="{StaticResource ImageListBoxStyle}"
                 HorizontalContentAlignment="Stretch"
                 PreviewMouseDown="ListBox_PreviewMouseDown">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Image Source="{Binding ImgSource}" 
                               Width="64" Height="64" Margin="4"
                               HorizontalAlignment="Center"/>

                            <TextBlock Grid.Row="1" 
                                   Text="{Binding Title}" 
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"
                                   TextWrapping="NoWrap"
                                   ToolTip="{Binding Title}"
                                   Margin="4,0,4,4"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding ChangeImageCommand}" CommandParameter="{Binding ElementName=imgListBox, Path=SelectedItem}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ListBox>

            <TextBlock Grid.Row="2" Margin="10" Foreground="{DynamicResource MaterialDesignBody}" Text="{Binding ModelTrainService.Message}"/>
        </Grid>

        <Grid Grid.Column="1" Margin="0, 10, 0, 0">
            <TabControl>
                <TabItem Header="{DynamicResource ModelTraining}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <ScrollViewer VerticalScrollBarVisibility="Hidden">
                            <StackPanel Margin="2, 10, 0, 0">
                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource InputOutputPaths}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <Button Content="{DynamicResource OriginImgDir}" Margin="10, 10, 15, 10" Command="{Binding ModelTrainService.LoadOriginImgDirCommand}"/>
                                            <TextBox IsReadOnly="True" Margin="10, 0, 15, 15" Text="{Binding ModelTrainService.OriginImgDir}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <Button Content="{DynamicResource SegmentationDir}" Margin="10, 0, 15, 10" Command="{Binding ModelTrainService.LoadSegmentationDirCommand}"/>
                                            <TextBox IsReadOnly="True" Margin="10, 0, 15, 15" Text="{Binding ModelTrainService.SegmentationDir}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <Button Content="{DynamicResource PretrainedModelFile}" Margin="10, 0, 15, 10" Command="{Binding ModelTrainService.LoadPretrainedModelFileCommand}"/>
                                            <TextBox IsReadOnly="True" Margin="10, 0, 15, 15" Text="{Binding ModelTrainService.PretrainedModelFile}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <Button Content="{DynamicResource TargetRootDir}" Margin="10, 0, 15, 10" Command="{Binding ModelTrainService.LoadTargetRootDirCommand}"/>
                                            <TextBox IsReadOnly="True" Margin="10, 0, 15, 15" Text="{Binding ModelTrainService.TargetDir}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>

                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource SplitDataSet}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource TrainingPercent}" Margin="5, 6, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.TrainingPercent}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource ValidationPercent}" Margin="5, 0, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.ValidationPercent}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>

                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource DefectInfo}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <Button Content="{DynamicResource DefectInfoWindow}" Margin="10, 10, 15, 10" Command="{Binding ModelTrainService.OpenDefectInfoWindowCommand}"/>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>

                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource PreprocessingParamsBeforeTrain}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource ImageScaledWidth}" Margin="5, 6, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.PreProcessingParam.ImageScaledWidth}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource ImageScaledHeight}" Margin="5, 0, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.PreProcessingParam.ImageScaledHeight}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource ImageScaledNumChannels}" Margin="5, 0, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.PreProcessingParam.ImageScaledNumChannels}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource ImageRangeMin}" Margin="5, 0, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.PreProcessingParam.ImageRangeMin}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource ImageRangeMax}" Margin="5, 0, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.PreProcessingParam.ImageRangeMax}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource NormalizationType}" Margin="5, 0, 10, 5"/>
                                            <ComboBox Text="{Binding ModelTrainService.PreProcessingParam.NormalizationType}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedComboBox}" Padding="5">
                                                <ComboBoxItem>none</ComboBoxItem>
                                                <ComboBoxItem>all_channels</ComboBoxItem>
                                                <ComboBoxItem>first_channel</ComboBoxItem>
                                                <ComboBoxItem>constant_values</ComboBoxItem>
                                            </ComboBox>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>

                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource TrainingParams}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource BatchSizeDuringTraining}" Margin="5, 6, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.TrainningParam.BatchSize}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource LearningRate}" Margin="5, 0, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.TrainningParam.LearningRate}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource Momentum}" Margin="5, 0, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.TrainningParam.Momentum}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource NumEpochs}" Margin="5, 0, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.TrainningParam.NumEpochs}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <TextBlock Text="{DynamicResource EvaluationIntervalEpochs}" Margin="5, 0, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.TrainningParam.EvaluationIntervalEpochs}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>

                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource RandSeed}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource RandSeed}" Margin="5, 6, 10, 5"/>
                                            <TextBox Text="{Binding ModelTrainService.SeedRand}" Margin="5, 0, 10, 10" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>
                            </StackPanel>
                        </ScrollViewer>

                        <StackPanel Grid.Row="1" Margin="0,10">
                            <Button Content="{DynamicResource Train}" Command="{Binding ModelTrainService.TrainCommand}" IsEnabled="{Binding ModelTrainService.IsTrainBtnEnabled}"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <TabItem Header="{DynamicResource ImageDetection}" FontSize="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <ScrollViewer VerticalScrollBarVisibility="Hidden">
                            <StackPanel Margin="2, 10, 0, 0">
                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource ImgSrc}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <Button Content="{DynamicResource LoadImage}" Margin="10, 10, 15, 10" Command="{Binding LoadImagesCommand}"/>
                                            <TextBox IsReadOnly="True" Margin="10, 0, 15, 15" Text="{Binding LoadedImgsArray}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <Button Content="{DynamicResource LoadDirectory}" Margin="10, 0, 15, 10" Command="{Binding LoadDirectoryCommand}"/>
                                            <TextBox IsReadOnly="True" Margin="10, 0, 15, 15" Text="{Binding ImgSrcDirectoryName}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>

                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource LoadInferenceFiles}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <Button Content="{DynamicResource LoadPreprocessParamDict}" Margin="10, 10, 15, 10" Command="{Binding LoadPreprocessParamDictCommand}"/>
                                            <TextBox IsReadOnly="True" Margin="10, 0, 15, 15" Text="{Binding PreprocessParamDictFileName}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                            <Button Content="{DynamicResource LoadTrainedModel}" Margin="10, 0, 15, 10" Command="{Binding LoadTrainedModelCommand}"/>
                                            <TextBox IsReadOnly="True" Margin="10, 0, 15, 15" Text="{Binding TrainedModelFileName}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5"/>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>

                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource InferenceParams}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource BatchSize}" Margin="5, 6, 10, 5" Foreground="{DynamicResource MaterialDesignBody}" />
                                            <TextBox Text="{Binding DefectDetectionService.BatchSize, Mode=TwoWay}" Foreground="{DynamicResource MaterialDesignBody}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5" Margin="5, 0, 10, 10"/>
                                            <TextBlock Text="{DynamicResource FiltrationArea}" Margin="5, 0, 10, 5" Foreground="{DynamicResource MaterialDesignBody}" />
                                            <TextBox Text="{Binding DefectDetectionService.FiltrationArea, Mode=TwoWay}" Foreground="{DynamicResource MaterialDesignBody}" Style="{DynamicResource MaterialDesignOutlinedTextBox}" Padding="5" Margin="5, 0, 10, 10"/>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>

                                <Expander Foreground="{DynamicResource MaterialDesignBody}" FontWeight="Bold" FontSize="14">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal" Margin="-18, 0">
                                            <TextBlock Text="{DynamicResource ResultExport}" FontWeight="Bold" FontSize="18"/>
                                        </StackPanel>
                                    </Expander.Header>
                                    <Expander.Content>
                                        <StackPanel>
                                            <Button Content="{DynamicResource ExportExcel}" Margin="10, 10, 15, 10" Command="{Binding ExportExcelCommand}"/>
                                            <Button Content="{DynamicResource ExportDetectionImg}" Margin="10, 0, 15, 15" Command="{Binding ExportDetectionImgCommand}"/>
                                        </StackPanel>
                                    </Expander.Content>
                                </Expander>
                            </StackPanel>
                        </ScrollViewer>
                        
                        <StackPanel Grid.Row="1" Margin="0,10">
                            <Button Content="{DynamicResource Run}" Command="{Binding RunCommand}"/>
                        </StackPanel>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
